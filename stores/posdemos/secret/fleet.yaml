# Bundle para desplegar Secrets (ACR + cloudflared) en el cluster posdemos.
defaultNamespace: poslite
targetCustomizations:
- clusterSelector:
    matchLabels:
      atlas: "true"
      store: "posdemos"
